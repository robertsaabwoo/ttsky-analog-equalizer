.DS_Store
.idea
.vscode
*.vcd
runs
tt_submission
src/user_config.tcl
test/sim_build
test/__pycache__/
test/results.xml
test/gate_level_netlist.v

# --- simulation outputs & data dumps (regenerable from sources; kept out per CLAUDE.md VM rules) ---
*.raw
*.out
xschem/vin.txt
xschem/vip.txt
xschem/tuning/*.log
xschem/tuning/ctle/*.log
xschem/simulation/
xschem/tuning/c25_batch.log

# Unrelated scratch that lives in this working tree but is not part of the
# project: a separate magic exercise, and the per-cell LVS reports written by
# mag/lvs_cell.sh.
magic_challenge/
